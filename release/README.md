# Release packaging

A user-facing release should contain two downloads:

- Android APK
- Windows x64 ZIP containing `CustomController.Server.exe` and the `installer/` scripts

The server EXE is intentionally excluded from the Git repository and should be attached to the GitHub Release instead.
